#pragma once

#include <stdint.h>

/**
 * The SSID for the network
*/
const char * ssid = "WirelessAirLeak";

/**
 * The password for the network.
*/
const char * password = "wireless-air-leak";


/**
 * The Host IP for the computer application.
*/
const char * host_name = "192.168.1.2";
/**
 * The TCP Port for the computer application.
*/
const uint16_t tcp_port = 50007;